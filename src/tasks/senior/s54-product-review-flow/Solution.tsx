import type { ProductReviewFlowProps } from './types';
import styles from './Solution.module.css';

// ⬇ Delete this line when you start. Tests for this task are skipped while it exists.
export const NOT_STARTED = true;

export default function ProductReviewFlow({ api, storageKey = 'review-drafts' }: ProductReviewFlowProps) {
  // Your implementation here. Requirements are in README.md.
  void api;
  void storageKey;
  return <div className={styles.root}>Product review workflow: start coding in Solution.tsx</div>;
}
