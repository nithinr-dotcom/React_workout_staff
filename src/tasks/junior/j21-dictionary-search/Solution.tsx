import type { DictionarySearchProps } from './types';
import styles from './Solution.module.css';

// ⬇ Delete this line when you start. Tests for this task are skipped while it exists.
export const NOT_STARTED = true;

export default function DictionarySearch({ searchAsYouTypeMs }: DictionarySearchProps) {
  // Your implementation here. Requirements are in README.md.
  // Fetch definitions with `lookupWord` from '../../../mocks/api'.
  void searchAsYouTypeMs;
  return <div className={styles.root}>Dictionary Search: start coding in Solution.tsx</div>;
}
